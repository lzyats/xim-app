import 'package:flutter/material.dart';

// 聊天=消息=其他
class ChatMessageOther extends StatelessWidget {
  const ChatMessageOther({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text(
      '[未知的消息类型]',
    );
  }
}
